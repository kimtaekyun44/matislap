# 제퍼디쇼

> ⚠️ **이 문서는 구현 전 설계 초안입니다. 실제 스키마와 다릅니다.**
>
> 아래 3장(Data Model)의 테이블·컬럼명은 실제 DB와 일치하지 않습니다.
> DB 구조는 반드시 [database-schema.sql](database-schema.sql)을 참고하세요.
>
> | 이 문서 | 실제 |
> |---|---|
> | `rooms` | `game_rooms` |
> | `questions` | `jeopardy_questions` |
> | `participants` | `game_participants` |
> | `code`, `host_id` | `room_code`, `instructor_id` |
> | `current_question_id` | `current_jeopardy_question_id` |
> | `buzzer_winner_id` | `jeopardy_buzzer_winner_id` |
> | `user_nickname` | `nickname` |
>
> 실제 `jeopardy_questions`에는 이 문서에 없는 `question_type`, `options`,
> `is_used`, `order_num`, `room_id` 컬럼이 있습니다.
> 인증도 소셜 로그인이 아닌 Custom JWT로 구현되었습니다.

# 📝 Project Spec: Real-time Jeopardy Web Service

## 1. Project Overview

- **목적**: 관리자가 퀴즈를 출제하고 참여자가 실시간으로 버저를 눌러 맞히는 웹 기반 제퍼디쇼 플랫폼.
- **핵심 가치**: 저지연(Low Latency) 동기화, 공정한 버저 선점 로직, 실시간 스코어보드.

## 2. Tech Stack

- **Frontend**: Next.js 14+ (App Router), Tailwind CSS, Lucide React (Icons).
- **Backend/BaaS**: Supabase
    - **Auth**: Google/Kakao 소셜 로그인 및 익명 로그인.
    - **Database**: PostgreSQL.
    - **Realtime**: Broadcast (화면 전환), Presence (접속자 관리), Postgres Changes (점수 업데이트).
- **Deployment**: Vercel.

## 3. Data Model (Schema)

## `rooms` (게임방)

| **Column** | **Type** | **Description** |
| --- | --- | --- |
| `id` | uuid (PK) | 방 고유 식별자 |
| `code` | string | 참여용 PIN 코드 (6자리) |
| `host_id` | uuid | 방장(관리자) ID |
| `status` | enum | WAITING, PLAYING, FINISHED |
| `current_question_id` | uuid | 현재 활성화된 문제 ID |
| `buzzer_winner_id` | uuid | 현재 문제의 버저 선점자 (Nullable) |

## `questions` (문제 데이터)

| **Column** | **Type** | **Description** |
| --- | --- | --- |
| `id` | uuid (PK) | 문제 식별자 |
| `category` | string | 카테고리 명 |
| `points` | int | 점수 (200, 400, 600 등) |
| `content` | text | 퀴즈 내용 |
| `answer` | text | 정답 |

## `participants` (참여자)

| **Column** | **Type** | **Description** |
| --- | --- | --- |
| `id` | uuid (PK) | 참여 식별자 |
| `room_id` | uuid | 소속된 방 ID |
| `user_nickname` | string | 참여자 닉네임 |
| `score` | int | 현재 점수 (기본값 0) |

## 4. Key Workflows & Logic

## 1) 실시간 화면 동기화 (Broadcast)

- 관리자가 특정 문제를 클릭하면 `supabase.channel('room_id').send(...)`를 통해 모든 클라이언트에게 `CHANGE_SCREEN` 이벤트를 발생시킴.
- 참여자의 Next.js 클라이언트는 해당 이벤트를 수신하여 `QuestionView` 컴포넌트로 즉시 전환.

## 2) 원자적 버저 선점 로직 (The Race)

- **동시성 해결**: 클라이언트의 시간값이 아닌 DB의 **Atomic Update**를 활용.
- **SQL 로직**:SQL
    
    `UPDATE rooms 
    SET buzzer_winner_id = 'user_id' 
    WHERE id = 'room_id' AND buzzer_winner_id IS NULL;`
    
- `affected_rows`가 1이면 선점 성공, 0이면 실패로 처리하여 UI에 즉각 반영.

## 3) 점수 관리

- 관리자가 유저의 답변을 듣고 '정답' 혹은 '오답' 버튼 클릭.
- DB의 `participants.score`가 업데이트되면 `Postgres Changes` 구독을 통해 모든 참여자의 스코어보드가 실시간으로 갱신됨.

## 5. UI/UX Phases

- **Phase 1: Lobby**: 방 생성/입장 및 참여자 리스트 확인.
- **Phase 2: Game Board**: 5x5 또는 5x6 형태의 제퍼디 격자판 (관리자만 클릭 가능).
- **Phase 3: Question Mode**: 큰 글씨로 문제 노출 + 하단에 거대 버저 버튼.
- **Phase 4: Result**: 최종 순위 및 우승자 발표 효과.

---

## 6. Development Roadmap

- [ ]  Supabase 프로젝트 생성 및 스키마 설정
- [ ]  Next.js 프로젝트 초기화 및 실시간 채널 연결 테스트
- [ ]  관리자용 문제 선택 및 화면 전환 기능 구현
- [ ]  버저 선점 로직 (Race Condition) 최적화
- [ ]  점수 가감 및 실시간 스코어보드 UI 완성