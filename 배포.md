# 배포 가이드

## 환경
- 서버: AWS EC2 (Ubuntu)
- 경로: `/var/apps/matislap`
- 프로세스 관리: PM2
- 저장소: GitHub (`origin/master`)

---

## 배포 순서

### 1. 로컬에서 GitHub에 Push
```bash
git push origin master
```

### 2. EC2 서버 접속 후 실행
```bash
cd /var/apps/matislap
git pull origin master
npm install        # package.json 변경이 있을 때만 필요
npm run build
pm2 restart all
```

> **npm install은 언제?**
> `package.json` 또는 `package-lock.json`이 변경된 커밋이 포함된 경우에만 실행. 변경이 없으면 생략 가능.

---

## DB 마이그레이션
코드 변경과 함께 DB 스키마가 변경된 경우, **빌드 전에** Supabase SQL Editor에서 마이그레이션 SQL을 실행해야 합니다.

### 마이그레이션 이력
| 날짜 | 내용 | SQL |
|------|------|-----|
| 2026-03-28 | 제퍼디쇼 버저 답변 컬럼 추가 | `ALTER TABLE game_rooms ADD COLUMN IF NOT EXISTS jeopardy_buzzer_answer text;` |
| 2026-08-11 | 퀴즈 문제 변경 이력 테이블 추가 | 아래 `create_quiz_question_history` 참조 |
| 2026-08-13 | 사다리 자동 생성 항목 구분 컬럼 | `alter table ladder_items add column if not exists is_auto boolean not null default false;` |
| 2026-08-13 | 재진입 승인 요청 테이블 | `database-schema.sql` 의 `rejoin_requests` 참조 |

> 개발과 운영이 **같은 Supabase 프로젝트**를 사용하므로, 위 마이그레이션은 개발 중 적용되면
> 운영에도 즉시 반영됩니다. 배포 시 별도로 SQL을 실행할 필요가 없습니다.

#### `create_quiz_question_history`
```sql
create table if not exists public.quiz_question_history (
  id uuid primary key default uuid_generate_v4(),
  question_id uuid not null,   -- 문제 삭제 후에도 이력 보존을 위해 FK 없음
  room_id uuid not null references public.game_rooms(id) on delete cascade,
  instructor_id uuid,
  instructor_name varchar(100),
  action varchar(20) not null check (action in ('create', 'update', 'delete')),
  room_status varchar(20),     -- 변경 시점의 방 상태
  order_num integer,
  changed_fields jsonb not null default '{}'::jsonb,
  snapshot jsonb,
  created_at timestamptz not null default now()
);

create index if not exists idx_quiz_question_history_room
  on public.quiz_question_history (room_id, created_at desc);

create index if not exists idx_quiz_question_history_question
  on public.quiz_question_history (question_id, created_at desc);

alter table public.quiz_question_history enable row level security;
```

---

## 배포 전 체크리스트
- [ ] 로컬에서 빌드 오류 없는지 확인 (`npm run build`)
- [ ] DB 마이그레이션 필요 여부 확인
- [ ] GitHub push 완료 확인
- [ ] EC2에서 `git pull` 후 최신 커밋 해시 확인 (`git log --oneline -3`)
- [ ] `pm2 list`로 프로세스 정상 실행 확인
